# TODO

- [x] Edit `src/firebase.ts` to add Firebase Auth (Email/Password) and export `auth`.
- [x] Edit `src/App.tsx` to add Login/Registration UI (email + password).

- [ ] Add auth state handling (`onAuthStateChanged`) and protect student app views.
- [ ] Implement logout (Exit -> Logout).
- [ ] Update Firestore reads/writes to include `uid` for student-specific `results` and `attempts`.
- [ ] Update history/progress/dashboard queries to filter by `uid`.
- [ ] Build/test with `npm run build` and check TypeScript errors.

